<div class="container mx-auto flex items-center justify-between max-w-7xl py-6">
	<a href="{{ site.url }}" class="block flex-shrink-0">
		<img src="{{ site.theme.link }}/static/img/logo_mrsens_white.svg" alt="Monsieur Sens" class="h-24 w-auto">
	</a>

	<nav id="nav-main" class="flex-1" role="navigation" aria-label="Menu principal">
		{% if header_menu %}
			{% include "menu.twig" with {'items': header_menu.get_items} %}
		{% endif %}
	</nav>

	<div class="flex-shrink-0">
		<a href="{{ contact_url }}" class="inline-block bg-ms-gold text-ms-black py-2 px-6 rounded-lg font-semibold border-2 border-transparent hover:bg-ms-black hover:text-ms-gold hover:border-ms-gold transition duration-300">
			{{ _e('Contact', 'monsieur-sens') }}
		</a>
	</div>
</div>
