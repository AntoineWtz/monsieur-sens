<article class="suggestion-plat">
  <div class="overflow-hidden rounded-lg shadow-sm bg-white border">
    <div class="p-5">
      <h3 class="text-lg font-semibold text-gray-900">{{ plat.title }}</h3>
      {% if plat.excerpt %}
        <p class="mt-2 text-sm text-gray-600">{{ plat.excerpt }}</p>
      {% endif %}
    </div>
  </div>
</article>
